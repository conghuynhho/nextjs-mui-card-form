import { CoreSettings, CropperState, MoveDirections } from "../types/index";
type MoveAlgorithm<Settings extends CoreSettings = CoreSettings> = (state: CropperState, settings: Settings, directions: MoveDirections) => CropperState;
declare function moveCoordinates<Settings extends CoreSettings>(state: CropperState, settings: Settings, directions: MoveDirections): CropperState;
export { MoveAlgorithm, moveCoordinates };
