import { Boundary, CoreSettings, CropperState, ImageSize, PartialTransforms, Priority } from "../types/index";
interface CreateStateOptions {
    boundary: Boundary;
    imageSize: ImageSize;
    transforms?: PartialTransforms;
    priority?: Priority;
}
type CreateStateAlgorithm<Settings extends CoreSettings = CoreSettings> = (options: CreateStateOptions, settings: Settings) => CropperState;
declare function createState(options: CreateStateOptions, settings: CoreSettings): CropperState;
export { CreateStateOptions, CreateStateAlgorithm, createState };
