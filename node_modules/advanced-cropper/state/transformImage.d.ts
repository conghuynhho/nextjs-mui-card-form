import { CropperState, CoreSettings, ImageTransform, ModifierSettings } from "../types/index";
type TransformImageAlgorithm<Settings extends CoreSettings & ModifierSettings = CoreSettings & ModifierSettings> = (state: CropperState, settings: Settings, transform: ImageTransform) => CropperState;
declare function transformImage(state: CropperState, settings: CoreSettings & ModifierSettings, transform: ImageTransform): CropperState;
export { TransformImageAlgorithm, transformImage };
