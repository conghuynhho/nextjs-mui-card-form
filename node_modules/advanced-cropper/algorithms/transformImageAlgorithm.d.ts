import { CropperState, CoreSettings, ImageTransform, ModifierSettings } from "../types/index";
declare function transformImageAlgorithm(state: CropperState, settings: CoreSettings & ModifierSettings, transform: ImageTransform): CropperState;
export { transformImageAlgorithm };
