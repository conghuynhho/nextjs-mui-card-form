import { CoreSettings, CropperState, Rotate } from "../types/index";
type RotateImageAlgorithm = (state: CropperState, settings: CoreSettings, rotate: number | Rotate) => CropperState;
declare function rotateImageAlgorithm(state: CropperState, settings: CoreSettings, rotate: number | Rotate): CropperState;
export { RotateImageAlgorithm, rotateImageAlgorithm };
