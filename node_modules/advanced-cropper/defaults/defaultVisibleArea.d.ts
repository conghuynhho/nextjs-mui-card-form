import { CoreSettings, CropperState } from "../types/index";
declare function defaultVisibleArea(state: CropperState, settings: CoreSettings): import("../types/index").Coordinates;
export { defaultVisibleArea };
