import { CoreSettings, CropperState, InitializedCropperState } from "../types/index";
declare function fitVisibleArea(state: InitializedCropperState, settings: CoreSettings): InitializedCropperState;
declare function fitVisibleArea(state: CropperState, settings: CoreSettings): CropperState;
export { fitVisibleArea };
