import { CoreSettings, CropperState, PostprocessAction } from "../../types/index";
declare function preventZoom(state: CropperState, settings: CoreSettings, action: PostprocessAction): CropperState;
export { preventZoom };
