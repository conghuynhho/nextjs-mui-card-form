/// <reference types="react" />
import { CropperImage, CropperState, CropperTransitions } from 'advanced-cropper';
interface Props {
    state: CropperState | null;
    image: CropperImage | null;
    transitions?: CropperTransitions | null;
    className?: string;
    imageClassName?: string;
    contentClassName?: string;
}
export declare const CropperPreview: ({ className, contentClassName, imageClassName, state, image, transitions, }: Props) => JSX.Element;
export {};
