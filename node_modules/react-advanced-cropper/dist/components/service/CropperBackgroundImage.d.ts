import React from 'react';
import { CropperTransitions, CropperImage, CropperState } from 'advanced-cropper';
interface DesiredCropperRef {
    getState: () => CropperState;
    getTransitions: () => CropperTransitions;
    getImage: () => CropperImage;
}
interface Props {
    className?: string;
    cropper: DesiredCropperRef;
    crossOrigin?: 'anonymous' | 'use-credentials' | boolean;
}
export declare const CropperBackgroundImage: React.ForwardRefExoticComponent<Props & React.RefAttributes<HTMLImageElement>>;
export {};
