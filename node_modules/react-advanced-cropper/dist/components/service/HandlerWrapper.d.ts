import { CSSProperties, ReactNode } from 'react';
import { HorizontalCardinalDirection, MoveDirections, VerticalCardinalDirection } from 'advanced-cropper';
interface Props {
    className?: string;
    style?: CSSProperties;
    children?: ReactNode;
    onDrag?: (shift: MoveDirections, event: MouseEvent | TouchEvent) => void;
    onDragEnd?: () => void;
    onLeave?: () => void;
    onEnter?: () => void;
    disabled?: boolean;
    horizontalPosition?: HorizontalCardinalDirection;
    verticalPosition?: VerticalCardinalDirection;
}
export declare const HandlerWrapper: ({ horizontalPosition, verticalPosition, className, disabled, onDrag, onDragEnd, onLeave, onEnter, children, style, }: Props) => JSX.Element;
export {};
