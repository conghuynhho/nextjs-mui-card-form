import { FC } from 'react';
import { CropperTransitions } from 'advanced-cropper';
interface Props {
    className?: string;
    transitions?: CropperTransitions;
    width?: number;
    height?: number;
    left: number;
    top: number;
}
export declare const StencilWrapper: FC<Props>;
export {};
