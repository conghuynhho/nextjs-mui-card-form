/// <reference types="react" />
import { CardinalDirection, MoveDirections } from 'advanced-cropper';
interface Props {
    defaultClassName?: string;
    hoverClassName?: string;
    wrapperClassName?: string;
    position?: CardinalDirection;
    disabled?: boolean;
    onDrag?: (directions: MoveDirections, event: TouchEvent | MouseEvent) => void;
    onDragEnd?: () => void;
}
export declare const SimpleLine: ({ position, hoverClassName, wrapperClassName, defaultClassName, disabled, onDrag, onDragEnd, }: Props) => JSX.Element;
export {};
