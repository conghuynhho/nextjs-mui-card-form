import { ScaleImageSettings } from '../types';
interface UseScaleImageOptions {
    touch: boolean;
    wheel: boolean | {
        ratio: number;
    };
}
export declare function useScaleImageOptions(scaleImage: ScaleImageSettings | boolean): UseScaleImageOptions;
export {};
