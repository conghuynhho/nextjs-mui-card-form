import { DefaultSettings } from 'advanced-cropper/defaults';
import { CoreSettings, ModifierSettings } from 'advanced-cropper';
import { CropperInstance, AbstractCropperParameters, AbstractCropperCallbacks } from 'advanced-cropper/instance';
export declare type CropperStateSettings = DefaultSettings & CoreSettings & ModifierSettings;
export declare type CropperStateSettingsProp<Settings extends CropperStateSettings> = Partial<Pick<Settings, keyof CropperStateSettings>> & Omit<Settings, keyof CropperStateSettings>;
export declare function useCropperState<Settings extends CropperStateSettings, Instance = unknown>(props: () => AbstractCropperParameters<Settings> & AbstractCropperCallbacks<Instance> & {
    settings: CropperStateSettingsProp<Settings>;
}, settings: {
    autoReconcileState?: boolean;
}): CropperInstance<Settings, Instance>;
export declare type CropperStateHook = ReturnType<typeof useCropperState>;
